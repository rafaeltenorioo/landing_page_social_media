import { Lora, Playwrite_NO } from "next/font/google";

export const lora = Lora({
  subsets: ["latin"],
  display: "swap",
});

export const playwrite_nz_guides = Playwrite_NO({
  weight: "400",
  display: "swap",
});